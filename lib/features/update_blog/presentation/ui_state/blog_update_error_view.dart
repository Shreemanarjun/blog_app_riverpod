import 'package:flutter/material.dart';

class BlogUpdateErrorView extends StatelessWidget {
const BlogUpdateErrorView({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context){
    return Container();
  }
}